{
  "tasks": {
    "test": "cargo test",
    "build": "cargo build",
    "launch": "cargo run"
  },
  "postCreateCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source $HOME/.cargo/env"
}
